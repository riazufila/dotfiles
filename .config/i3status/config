# i3status configuration file.
# see "man i3status" for documentation.

# It is important that this file is edited as UTF-8.
# The following line should contain a sharp s:
# ß
# If the above line is not correctly displayed, fix your editor first!

general {
        colors = true
        interval = 1
}

order += "wireless _first_"
order += "ethernet _first_"
order += "cpu_usage"
order += "memory"
order += "disk /"
order += "battery all"
order += "tztime local"

wireless _first_ {
        format_up = "W: Up"
        format_down = "W: Down"
}

ethernet _first_ {
        format_up = "E: Up"
        format_down = "E: Down"
}

cpu_usage {
        format = "CPU 1: %cpu0, CPU 2: %cpu1, CPU 3: %cpu2, CPU 4: %cpu3"
}

memory {
        format = "%used/%total"
}

disk "/" {
        format = "%avail"
}

battery all {
        format = "%percentage"
}

tztime local {
        format = "%a %d-%m-%Y %H:%M:%S"
}
